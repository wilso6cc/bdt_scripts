The get_classifier.py will output a pickle file with a BDT trained on WW and DFDY.

The get_2D_plots.py will output plots comparing relations between WW/DFDY with ttbar distributions.

The get_efficiency_tables.py will output tables with the efficiency of each leading order process in the LHC.
